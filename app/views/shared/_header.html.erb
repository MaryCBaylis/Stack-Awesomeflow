<div id="header">
  <ul>
    <% if user_signed_in? %>
      <li><%= button_to "Log Out", destroy_user_session_path, method: :delete %></li>
    <% else %>
      <li><%= button_to "Register", new_user_registration_path, method: :get %></li>
      <li><%= button_to "Login", new_user_session_path, method: :get %></li>
    <% end %>
  </ul>
</div>
