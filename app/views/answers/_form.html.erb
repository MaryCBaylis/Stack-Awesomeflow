<%= form_for [@question, @answer] do |f| %>

  <% if @answer.errors.any? %>
    <ul>
      <% @answer.errors.full_messages.each do |e| %>
        <li><%= e %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit %>
<% end %>